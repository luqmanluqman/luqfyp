
<div class="page-main">
  <%= stylesheet_link_tag "style" %>
  <%= stylesheet_link_tag "component" %>
  <%= javascript_include_tag "application"%>

 	<div class="page-section">
     	<legend class="form-heading heading"> Dashboard </legend>
     		Welcome home, <%= @current_user.first_name %> <%= @current_user.last_name %> !<br>
			<%= link_to "Edit Profile", controller: "users", action: "edit", id: session[:user_id] %><br>
			<%= link_to "Change Password", controller: "users", action: "changepass", id: session[:user_id] %><br>
			<%= link_to "Logout", controller: "sessions", action: "destroy" %>
			<!-- Display parameters -->
			<!--<hr/>
			Inspecting Parameters
			<%=params.inspect%>
			Session ID is=<%=session[:user_id] %>
			<%= session.inspect %> -->

	</div>
</div>